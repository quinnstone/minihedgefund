# Used ONLY by scripts/register-commands.js (one-time slash-command registration).
# NOT used by the deployed Worker — Worker reads secrets via Wrangler.
# Copy to .env and fill in.

DISCORD_APP_ID=
DISCORD_BOT_TOKEN=
