[pytest]
# Restrict pytest to the canonical tests/ directory. Stale iCloud/Dropbox
# sync artifacts (e.g. "tests 2/") land in the repo root from time to time
# and would otherwise get collected + duplicate every test run.
testpaths = tests
